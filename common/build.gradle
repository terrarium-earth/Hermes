architectury {
    common(enabled_platforms.split(","))
}

dependencies {
    modImplementation "net.fabricmc:fabric-loader:$fabric_loader_version"
    modApi "com.teamresourceful.resourcefullib:resourcefullib-common-$minecraft_version:$resourcefullib_version"
}